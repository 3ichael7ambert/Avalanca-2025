/// @description carousel_create()
function carousel_create() {

	//creates an carousel index in line
	//sets element position in carousel queue
	//use it on Create of the object or in instance_create(); of the carousel creator\controller

	angle=0;
	output_x=0;
	deg = 360/(global.items)*global._id
	global._id += 1



}
